@model Noliktava2.Models.PurchaseModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>PurchaseModel</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Vendor.TelephoneNumber)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Vendor.TelephoneNumber)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.Date)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Date)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.IsCompleted)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.IsCompleted)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
